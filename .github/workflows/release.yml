name: "Staging: idmtools_calibra"

on:
  push:
    branches:
      # only run this on
      - dev
      - main
    paths:
      - 'idmtools_calibra/**'
      - 'requirements.txt'
      - 'dev_requirements.txt'
      - 'pyproject.toml'
      - 'setup.py'

jobs:
  release:
    name: "Release idmtools_calibra"
    runs-on: ubuntu-latest
    steps:
      - name: Set up Python
        uses: actions/setup-python@v1
        with:
          python-version: '3.10'
      - uses: actions/checkout@v2
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install build twine
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
      - name: Build and publish
        env:
          TWINE_REPOSITORY_URL: https://packages.idmod.org/api/pypi/idm-pypi-staging/
          TWINE_USERNAME: ${{ secrets.PYPI_STAGING_USERNAME }}
          TWINE_PASSWORD: ${{ secrets.PYPI_STAGING_PASSWORD }}
        run: |
          python -m build
          twine upload dist/*
